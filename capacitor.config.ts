import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.side',
  appName: 'side',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
